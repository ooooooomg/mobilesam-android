# 更新日志

本项目的所有显著变更按时间倒序记录。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.0] - 2026-08-04

### 修复

- **胶囊毛玻璃矩形 bug**：毛玻璃区域从屏幕坐标改为 root 本地坐标（消除状态栏偏移），并对模糊区域做圆角裁剪（20dp），不再露出直角矩形边缘
- **设置分割线黑色过粗**：2dp 蓝色 → 1dp 浅灰（`outline_variant`）

### 变更

- **主题蓝加深**：浅色 `#1E88E5` → `#1976D2`，深色 `#90CAF9` → `#64B5F6`

## [1.0.9] - 2026-08-04

### 变更

- **胶囊更透明**：顶部/底部胶囊不透明度 90% → 55%，配合毛玻璃更通透
- **设置二级页极简**：模型卡片只保留主名 + 「查看详情」，去掉副标 / 一句话 / 运行时参数行，极简列表

## [1.0.8] - 2026-08-04

### 新增

- **真毛玻璃效果**：顶部/底部胶囊背后的相机画面做下采样高斯模糊（OverlayView 区域模糊），零依赖、每帧更新，胶囊下是真实的模糊玻璃感

### 说明

- BlurView 依赖在构建环境无法获取（JitPack/Maven 坐标 404），改用纯原生 Canvas 下采样模糊实现，不受网络限制

## [1.0.7] - 2026-08-04

### 变更

- **设置二级页精简**：模型卡片只保留主名 + 技术副标 + 一句话介绍 + 「查看详情」；类型/精度/输入/速度/模型路径全部移到三级详情页展示
- **模型分割线更明显**：模型之间 1dp 灰线 → 2dp 主色淡蓝描边线

### 备注

- 尝试引入 BlurView 实现真毛玻璃：构建环境代理无法访问 JitPack/Maven 上的 `com.eightbitlab:blurview` 坐标（均 404），已回退保持半透明方案。真毛玻璃可后续在可联网环境下用 BlurView 或系统 RenderEffect（API 31+）接入

## [1.0.6] - 2026-08-04

## [1.0.5] - 2026-08-04

### 变更

- **主题色改浅蓝**：薰衣草紫（靛蓝系）→ 天空蓝（浅色 `#1E88E5` / 深色 `#90CAF9`），整体蓝白基调
- **玻璃更不透明**：顶部/底部胶囊不透明度提高（浅色 90% / 深色 85%）
- **移除胶囊横线**：顶部/底部胶囊顶部的玻璃高光线改为纯半透明底（去掉多余横线）
- **图标重绘**：主题蓝底 + 简约白色相机（去掉彩色蒙版泡泡），蓝白纯净风格

## [1.0.4] - 2026-08-04

### 修复（鲁棒性审查后）

- **修复 use-after-recycle 崩溃**：letterbox 尺寸未变时 `createScaledBitmap` 返回源帧，误 recycle 导致每帧崩溃，改为仅回收新建位图
- **OOM 不再崩溃**：analyzeFrame 捕获 `OutOfMemoryError`，跳过重帧让 GC 恢复
- **修复 mask anchor 匹配错位**：YOLO-seg 的 anchor 查找 key 从"取整坐标"改为"float 位模式"，避免多个 anchor 撞 key 取到错误掩码系数
- **tensor 泄漏修复**：SamMaskDecoder 的 session.run 异常时也释放输入 tensor（try/finally）
- **移除锁内 System.gc()**：模型切换不再拖慢 analyzer 线程

### 优化

- MaskComposer 无检测结果时跳过全帧 copy
- TwoStage 只缓存 scale 而非整个 1.7MB 预处理输入

## [1.0.3] - 2026-08-04

### 修复

- **底部胶囊方框 bug**：胶囊顶部高光线穿透到胶囊下半部形成"方框"感，改为 2dp 全圆角高光带

### 变更

- **玻璃更不透明**：胶囊半透明度降低（浅色 80% / 深色 70%），画面更清爽
- **整体浅蓝调**：表面 / 背景 / 胶囊 / 图例统一偏浅蓝（`#F6F8FC` 系）

## [1.0.2] - 2026-08-04

### 变更

- **顶部更简洁**：去掉模型名下的技术副标小字（YOLO11n / 384 等），只保留模型主名 + 箭头 + 翻转按钮
- **胶囊磨砂玻璃加强**：顶部/底部胶囊加顶部高光细线（玻璃反光），半透明度调整，质感更接近磨砂玻璃

## [1.0.1] - 2026-08-04

### 变更

- **取景铺满全屏**：相机预览从 fit-center 改为 FILL_CENTER 裁剪铺满，overlay 应用相同 center-crop 变换，蒙版与预览精确对齐，无留白带
- **去圆角全屏**：移除相机取景框圆角与描边，画面直铺到底
- **胶囊无边框**：顶部/底部胶囊与图例 chip 移除描边边框，改半透明实色底 + 投影表达层次

## [1.0.0] - 2026-08-04

### 新增

- **多模型支持**：可插拔 `Segmenter` 抽象（两段式 / 端到端 / 纯检测），5 个可切换模型：
  - 智能检测（YOLO11n / 384，纯框检测）
  - 快速分割（YOLO11n / 384）
  - 快速分割/节能（YOLO11n / 384 / INT8 量化）
  - 高清分割（YOLO11n / 640）
  - 极致分割（SAM / 384，YOLOv8n 检测 + MobileSAM 两段式）
- **模型选择弹窗**：居中 70% 宽，主名 + 技术副标双行，当前模型高亮
- **模型详情页**：独立页面展示架构 / 参数量 / 精度 / 计算量 / 适用场景，附论文与 GitHub 源码链接
- **前后摄像头切换**：一键翻转
- **分割画面保存**：保存当前分割帧到系统相册（MediaStore）
- **弹幕式类别图例**：识别到的物体类别以「● 中文名」胶囊形式显示，带滞留（3s）、上飘动画与相邻帧去重
- **中文类别映射**：COCO 80 类中文化
- **靛蓝双主题**：浅色纯白 + 深靛蓝 `#3F51B5`；深色近黑 + 亮靛蓝 `#9FA8DA`，跟随系统
- **圆角取景框**：相机预览 + 胶囊圆角统一 20dp
- **应用更名**：MobileSAM →「实时分割」
- **App 图标重绘**：靛蓝底 + 相机镜头 + 彩色蒙版

### 优化

- **YUV→RGB 整数运算**：以整数移位近似替代逐像素浮点乘加，提速约 2-3 倍
- **mask 重建 anchor 哈希查找**：消除 NMS 后逐 box 线性扫描，O(1) 查找掩码系数
- **buffer 复用**：检测 / 掩码解码 / 蒙版系数数组复用，减少 GC
- 端到端模型 mask 改为 box-local 绘制，仅画框内区域

### 修复

- 相机预览圆角在 TextureView 硬件合成下不生效的问题（强制硬件层裁剪）
- 模型加载失败时显示错误而非静默
- 多物体时弹幕图例遮挡取景画面的问题

### 其他

- ONNX Runtime CPU 线程调优：intra 4 / inter 1 / ALL_OPT
- 相册页 EXIF 方向修正

## [未发布] - 初始版本

- 相机实时分割（CameraX + MobileSAM 两段式）
- 相册图片分割
- MobileSAM（512px 编码器）+ YOLOv8n 检测
